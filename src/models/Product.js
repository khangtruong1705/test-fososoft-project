export class Product {
  constructor(image, name, price, oldPrice, discount) {
    this.image = image;
    this.name = name;
    this.price = price;
    this.oldPrice = oldPrice;
    this.discount = discount;
  }
}